import React from "react";
import {View} from "react-native";
import {Text} from "react-native-paper";

export const Tasks = () => {
    return (
        <View>
            <Text>Tasks</Text>
        </View>
    );
}